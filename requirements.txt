# Versions PINNED to the ones the thesis results were produced with — a
# different VADER version could change sentiment scores and fail the frozen
# self-test. run_summary.txt records the versions actually used in each run.

# Sentiment backend (the only one supported — the pipeline stops without it;
# cite Hutto & Gilbert 2014)
vaderSentiment==3.3.2

# Porter stemmer for the term-discovery step (Porter 1980)
nltk==3.9.2
