{
  "_readme": "Expected-output model for out/provider_mentions.csv (step 4, AGGREGATE) — the vendor-landscape barometer. A provider row only exists when the provider was mentioned at least once. pct_of_total_mentions is the provider's share of all provider mentions (same relative-weight logic as factor_salience.csv; sums to ~100 across rows, up to rounding).",
  "kind": "csv",
  "columns": ["provider", "category", "n_threads",
              "pct_of_total_mentions", "total_mentions"],
  "min_rows": 1,
  "unique_columns": ["provider"],
  "non_empty_columns": ["provider", "category"],
  "numeric_ranges": {
    "n_threads": {"min": 1},
    "pct_of_total_mentions": {"min": 0, "max": 100},
    "total_mentions": {"min": 1}
  }
}
