{
  "_readme": "Expected-output model for out/steps/step3b_factors.json (factors module). Entry shape ({factor_key: [label, mentions >= 1]}) and that every key exists in the config dictionary are checked in code (pipeline/checks.py check_module).",
  "kind": "json",
  "records_key": "threads",
  "min_records": 1,
  "required_fields": ["thread_id", "factors"]
}
